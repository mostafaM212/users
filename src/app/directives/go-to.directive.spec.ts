import { GoToDirective } from './go-to.directive';

describe('GoToDirective', () => {
  it('should create an instance', () => {
    const directive = new GoToDirective();
    expect(directive).toBeTruthy();
  });
});
